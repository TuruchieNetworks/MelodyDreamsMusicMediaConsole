type MelodyDreams @model @auth(rules: [{allow: public}]) {
  id: ID!
  firstname: String
  lastname: String
  email: AWSEmail!
  username: String!
  password: String!
  password2: String!
  image: String
  tracks: String
  follow: Boolean
  followers: Int
  like: Boolean
  likes: Int
}
